spring:
  cloud:
    config:
      profile: dev  #启用的环境名称
      label: master  #分支的名称
      name: search  #配置文件名称
      discovery:
        enabled: true
        service-id: mall-config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/
